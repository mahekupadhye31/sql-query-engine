[project]
name = "sqlengine"
version = "0.1.0"
description = "A minimal SQL query engine over a custom on-disk storage layer."
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=7.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
